{
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "mac": "git pull origin main && git submodule update --init --force --remote && git add . && git commit -m \"Message\" && git push origin main",
    "windows": "git pull origin main ; git submodule update --init --force --remote ; git add . ; git commit -m \"Message\" ; git push origin main"
  }
}